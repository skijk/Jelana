# Uses the shared cache directory /var/cache/jelana.
# Uppdate Stats every hour.
# Change /var/www/html to the path for your environment.
0 * * * * www-data /usr/bin/php /var/www/html/jelana/bin/refresh-dashboard.php >> /var/log/jelana.log 2>&1
